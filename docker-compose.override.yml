services:
  web:
    volumes:
      - .:/app
      - web_venv:/app/.venv
    environment:
      FLASK_APP: wsgi:app
    command: sh -c "uv sync --locked --group dev && flask db upgrade && flask run --host 0.0.0.0 --port 8000 --debug"

volumes:
  web_venv:
